# Ink TUI 交互设计

## 目标

`chat-codex` 的 TUI 是现有核心交互的展示层，不是第二套业务逻辑。

目标体验：

- 首页一眼看懂渠道、聊天绑定、权限和是否可以启动。
- 首页展示新 session 默认工作目录，避免在错误仓库里创建 Codex session。
- 所有对象列表支持方向键、数字编号和快捷键。
- 常见动作不需要记命令：添加微信、添加飞书、绑定 session、设置权限、启动服务。
- 常见动作必须在页面正文中显示为可选项；底部快捷键只是加速入口，不能作为唯一入口。
- 成功、失败和下一步显示在固定状态栏，用户不需要翻终端历史。
- TUI 不直接读写本地状态文件，不直接判断 session owner 冲突，不直接 new 具体渠道 adapter 做业务判断。

非目标：

- 不做网页式复杂布局。
- 不改变微信一个账号一个主聊天、飞书一个机器人多个私聊 `chat_id` 的产品模型。
- 不改变 session 唯一归属规则。

## 技术选择

第一版使用 Ink。

原因：

- 项目当前是 Node.js + TypeScript，Ink 可以直接进入现有构建链路。
- React 组件模型适合 dashboard、列表、详情、底部状态栏和弹窗。
- `useInput` 可以处理方向键、回车、Esc 和快捷键。
- Ink 的测试工具可以对组件输出做单元测试。

可选配套：

- `@inkjs/ui`：用于输入框、密码框、确认框、选择框等基础控件。
- `ink-testing-library`：用于 TUI 组件测试。

暂不采用：

- `blessed` / `react-blessed`：能力强但生态偏旧，React 版本约束和维护风险更高。
- `terminal-kit`：控制力强，但更偏命令式，和后续组件测试不如 Ink 顺。
- `OpenTUI`：能力很强，但当前 Node 支持还不适合作为本项目主线。

## 分层边界

```text
Ink TUI Components
        ↓
LauncherActions / ChannelActions / BindingActions
        ↓
State Store / Channel Config Store / Codex Session Discovery
        ↓
Bridge Runtime
```

TUI 组件允许：

- 展示 actions 返回的结构化状态。
- 处理键盘输入。
- 调用 actions。
- 显示加载中、成功、错误和下一步提示。

TUI 组件禁止：

- 直接写 `state/bridge/*.json`。
- 直接读写 `session-owners.json` 判断 owner 冲突。
- 直接 new `WeixinAdapter` 或 `FeishuAdapter` 做业务判断。
- 直接调用 Codex adapter 创建 session。
- 在组件里实现微信 pending binding 或飞书多 `chat_id` 绑定规则。

## 入口行为

推荐入口保持不变：

```bash
npm run chat-codex
```

默认行为：

- TTY 环境：进入 Ink TUI。
- 非 TTY 环境：走现有 prompt fallback 或直接报错提示需要交互。
- `--no-tui`：强制走普通 prompt 交互。
- `--no-interactive`：不进入交互，只按现有非交互规则检查并启动。

`npm run cli:chat-codex` 保持同等别名。

长期分工：

- TTY 默认使用 TUI，后续正常人工启动都优先走 TUI。
- 普通 prompt CLI 必须保留，用于 `--no-tui`、非 TTY、自动化脚本、远程日志环境和 TUI 故障排查。
- TUI 和 prompt CLI 必须调用同一套 actions/services，不能分叉出两套业务逻辑。

## 全局布局

第一版使用单屏列表型布局，避免复杂全屏工作台。

```text
┌ Chat Codex ─────────────────────────────────────────────┐
│ 状态: 待启动                                  权限: 审批 │
├─────────────────────────────────────────────────────────┤
│ 渠道                                                     │
│  微信 / wx-main        已连接        主聊天待生效         │
│  飞书 / default        已配置        等待私聊             │
│                                                         │
│ 聊天绑定                                                 │
│  微信 / wx-main / 主聊天      你好呀 / 019e3024           │
│  飞书 / default / 张三        未绑定                      │
│                                                         │
│ 工作目录                                                 │
│  /Users/xiaohuang/codex-wechat/codex-openclaw-wechat     │
│                                                         │
│ 提示                                                     │
│  配置好后按 Enter 启动服务。                             │
├─────────────────────────────────────────────────────────┤
│ Enter 启动  c 渠道  b 绑定  p 权限  d 目录  r 刷新  q 退出 │
└─────────────────────────────────────────────────────────┘
```

窄终端：

- 使用单列。
- 长标题、长 ID、长路径省略。
- 不让 footer 被正文挤出屏幕。

宽终端：

- 可以在列表页使用左右两栏。
- 左栏是对象列表，右栏是当前项详情。
- 不要求第一版必须实现左右栏。

## 全局键盘

全局键：

- `Up` / `Down`：移动当前选中项。
- `Enter`：执行当前主操作，或进入当前选中项。
- `Esc`：返回上一级。
- `q`：返回上一级；在首页退出。
- `r`：刷新当前页面数据。
- `?`：显示当前页面快捷键说明。

首页快捷键：

- `Enter`：启动服务。如果当前不能启动，进入需要处理的页面。
- `c`：管理渠道。
- `b`：聊天绑定。
- `p`：默认权限设置。
- `d`：新 session 默认工作目录。
- `s`：状态详情。
- `q`：退出。

渠道页快捷键：

- `w`：添加微信账号。
- `f`：添加飞书机器人。
- `Enter`：进入选中渠道详情。
- `e`：启用或停用选中渠道。
- `Esc` / `q`：返回首页。

快捷键展示规则：

- Footer 只用于提示快捷键和当前状态，不承担功能发现。
- 添加微信、添加飞书、启用/停用、更新凭证、状态详情、返回等重要动作必须同时出现在页面正文的“操作”列表里，用户只用方向键和 `Enter` 也能完成。
- 有对象列表的页面可以先列对象，再列“操作”分组；数字选择对象时，操作项仍要能通过方向键选中。
- 对多渠道管理尤其如此：添加第二个、第三个微信账号或飞书机器人，不能只靠 `w` / `f` 两个隐藏快捷键。

绑定页快捷键：

- `Enter`：进入选中聊天绑定详情。
- `n`：给选中聊天新建并绑定 session。
- `m`：手动输入 session ID 绑定选中聊天。
- `u`：解绑选中聊天当前 session，需二次确认。
- `p`：设置选中聊天当前 session 权限。
- `Esc` / `q`：返回首页。

Session 选择页快捷键：

- `Enter`：绑定当前选中的可选 session。
- 数字：直接选择对应 session。
- `n`：新建 session。
- `m`：手动输入 session ID。
- `Esc` / `q`：返回。

## 页面状态

每个页面统一维护这些 UI 状态：

- `idle`：正常展示。
- `loading`：正在刷新、检查状态、创建 session 或启动服务。
- `input`：正在输入文本或 secret。
- `confirm`：等待用户二次确认。
- `error`：操作失败，底部显示错误，仍停留当前页面。
- `success`：操作成功，底部显示结果和下一步。

底部状态栏规则：

- 成功：显示绿色短句，例如 `已绑定 session：你好呀 / 019e3024`。
- 错误：显示红色短句，例如 `无法绑定：该 session 已绑定到 飞书 / default / 张三`。
- 提示：显示灰色或普通文本，例如 `启动服务后，在飞书里私聊机器人会自动记录 chat_id`。
- 密钥、token、secret 不显示在状态栏。

## 首页

首页展示五块：

- 渠道：每个渠道实例的类型、账号、连接状态和关键下一步。
- 聊天绑定：微信主聊天状态、飞书已发现私聊数量、未绑定数量。
- 权限：新 session 默认权限。
- 工作目录：新 session 默认工作目录，长路径省略，详情页完整展示。
- 运行提示：是否可以启动，不能启动时显示需要处理的第一件事。

无渠道时：

```text
┌ Chat Codex ─────────────────────────────────────────────┐
│ 状态: 未配置                                  权限: 审批 │
├─────────────────────────────────────────────────────────┤
│ 渠道                                                     │
│  暂无渠道                                                │
│                                                         │
│ 默认配置                                                 │
│  新 session 工作目录: /path/to/project                    │
│  新 session 权限: 审批模式                                │
│                                                         │
│ 操作                                                     │
│ > 1. 添加微信账号                  扫码登录后绑定主聊天   │
│   2. 添加飞书机器人                输入凭证并校验连通性   │
│   3. 权限设置                      审批模式               │
│   4. 工作目录                      /path/to/project       │
│   0. 退出                                                │
├─────────────────────────────────────────────────────────┤
│ Enter 执行   1/w 微信   2/f 飞书   3/p 权限   4/d 目录    │
└─────────────────────────────────────────────────────────┘
```

启动规则：

- 有至少一个 enabled 且 connected 的渠道时，`Enter` 进入启动确认。
- 没有渠道时，`Enter` 执行当前选中的显式操作；默认选中“添加微信账号”。
- 有渠道但不可启动时，`Enter` 跳到第一个需要处理的渠道详情。

## 管理渠道

渠道列表展示：

```text
┌ 管理渠道 ───────────────────────────────────────────────┐
│ 已配置渠道                                               │
│ > 微信 / wx-main        已启用   已连接                   │
│   飞书 / default        已启用   需要配置                 │
│                                                         │
│ 详情                                                     │
│  飞书机器人缺少本次运行凭证。重启后需环境变量或重新输入。 │
│                                                         │
│ 操作                                                     │
│   添加微信账号                                           │
│   添加飞书机器人                                         │
│   启用/停用选中渠道                                      │
│   查看选中渠道详情                                       │
│   返回首页                                               │
├─────────────────────────────────────────────────────────┤
│ Enter 详情   w 添加微信   f 添加飞书   e 启停   Esc 返回  │
└─────────────────────────────────────────────────────────┘
```

规则：

- “添加微信账号”和“添加飞书机器人”必须是正文里的可选操作项，不能只放在 footer 快捷键里。
- 已经存在渠道时仍然可以继续添加更多渠道实例，支持多微信账号、多飞书机器人并存。
- 渠道列表与操作列表可以在同一页上下分区；如果终端空间不足，优先保留当前选中渠道、添加渠道操作和返回操作。

渠道详情：

- 微信：
  - 登录或重新登录。
  - 设置微信主聊天绑定。
  - 查看状态详情。
  - 启用/停用。
- 飞书：
  - 查看配置状态。
  - 输入/更新本进程凭证。
  - 查看状态详情。
  - 启用/停用。
  - 不提供渠道级 session 绑定。

## 添加微信账号

流程：

1. TUI 调用 channel setup action 发起扫码登录。
2. 页面显示二维码和备用链接。
3. 用户扫码并手机确认。
4. 按 `Enter` 检查登录结果。
5. 按 `Esc` / `q` 返回管理渠道，不添加账号。
6. 登录成功后自动进入微信主聊天绑定页。

页面：

```text
┌ 添加微信账号 ───────────────────────────────────────────┐
│ 请使用微信扫码，并在手机上确认。                         │
│                                                         │
│  <二维码区域>                                            │
│                                                         │
│ 状态: 等待确认                                           │
├─────────────────────────────────────────────────────────┤
│ Enter 检查登录结果   Esc 返回                            │
└─────────────────────────────────────────────────────────┘
```

登录结果：

- 成功：注册微信渠道实例，底部显示账号和下一步。
- 超时：停留当前页，提示继续按 `Enter` 检查或返回。
- 失败：停留当前页或返回渠道页，显示失败原因。

## 添加飞书机器人

流程：

1. TUI 直接进入手动输入表单。
2. 输入 App ID，必填。
3. 输入 App Secret，必须用密码框隐藏。
4. 输入账号标识，必填。它是本地展示名称/实例标识，用来区分多个飞书机器人，例如 `公司机器人`、`研发助手`、`dalongxia`。
5. 飞书域默认使用 `feishu`，普通用户不需要配置；只有接入 Lark 国际版或特殊域时才进入高级输入。
6. 最后一项按 `Enter` 后，调用 action 校验渠道是否可通。
7. 校验成功后注册飞书渠道实例；校验失败则停留在表单并显示中文错误。

规则：

- App Secret 只保存在当前进程内存里。
- 不写入 state 文件。
- 不显示在页面、状态栏、错误文案和日志里。
- 重启后需要环境变量或重新手动添加。
- 添加成功后不进入 session 绑定。
- 账号标识不是 secret，可以持久化；它必须用户可辨认，不能只依赖 `default`。
- 飞书域不是主流程必填项。默认 `feishu`，高级场景才显示或允许修改。
- 连通性校验应至少验证凭证完整、机器人身份可获取、adapter 状态为 connected；如果无法连接，不应把渠道登记为可启动。

页面：

```text
┌ 添加飞书机器人 ─────────────────────────────────────────┐
│ App ID: cli_xxx                                         │
│ App Secret: ********                                    │
│ 账号标识: dalongxia                                     │
│ 飞书域: feishu（默认）                                  │
│                                                         │
│ > 保存并校验连通性                                      │
│   修改 App ID                                           │
│   修改 App Secret                                       │
│   修改账号标识                                          │
│   高级：修改飞书域                                      │
│   返回                                                  │
├─────────────────────────────────────────────────────────┤
│ Enter 执行   Secret 不回显   Esc 返回                    │
└─────────────────────────────────────────────────────────┘
```

提交校验状态：

```text
正在校验飞书机器人连通性...
```

校验失败示例：

```text
飞书机器人不可用：App ID 或 App Secret 不正确。请修改后重新校验。
```

成功页提示：

```text
飞书机器人已添加。
启动服务后，让用户在飞书里私聊机器人。
每个飞书私聊会按 chat_id 生成独立聊天绑定。
```

## 聊天绑定

绑定列表展示所有已发现聊天和 pending 微信主聊天绑定。

```text
┌ 聊天绑定 ───────────────────────────────────────────────┐
│ > 微信 / wx-main / 主聊天      待生效: 你好呀 / 019e3024 │
│   飞书 / default / 张三        未绑定                    │
│   飞书 / default / 李四        日报任务 / 019e2ea9        │
│                                                         │
│ 详情                                                     │
│  当前项: 飞书私聊。只能给这个 chat_id 绑定 session。      │
├─────────────────────────────────────────────────────────┤
│ Enter 详情   n 新建   m 手动绑定   u 解绑   p 权限        │
└─────────────────────────────────────────────────────────┘
```

显示规则：

- 微信主聊天使用 `微信 / <accountId> / 主聊天`。
- 未发现真实微信 route 但有 pending binding 时，显示 `待生效`。
- 飞书使用 `飞书 / <accountId> / <昵称或脱敏 ID>`。
- 不在列表主文案暴露完整 `routeKey`。
- 详情页可以显示完整 routeKey，用于排查。

## 绑定详情

```text
┌ 绑定详情 ───────────────────────────────────────────────┐
│ 聊天: 飞书 / default / 张三                              │
│ 当前 session: 未绑定                                     │
│ 当前权限: 使用默认权限                                   │
│ 最近消息: 2026-05-16 12:28                               │
│                                                         │
│ 操作                                                     │
│ > 选择已有 session                                       │
│   新建并绑定 session                                     │
│   设置当前 session 权限                                  │
│   解绑当前 session                                       │
├─────────────────────────────────────────────────────────┤
│ Enter 执行   Esc 返回                                    │
└─────────────────────────────────────────────────────────┘
```

规则：

- 当前聊天未绑定 session 时，`设置当前 session 权限` 和 `解绑` 显示为不可用。
- 新建 session 使用当前默认权限和当前工作目录。
- 绑定成功后底部显示聊天、session 和工作目录。
- owner 冲突由 actions 返回，TUI 只展示中文错误。

## Session 选择

```text
┌ 选择 Codex session ────────────────────────────────────┐
│ 聊天: 飞书 / default / 张三                              │
│                                                         │
│ 可选                                                     │
│ > 1. 可用   CLI 交互重构                 019e2ea9        │
│   2. 可用   微信登录优化                 019e2ca0        │
│                                                         │
│ 不可选                                                   │
│   已绑定到 飞书 / default / 李四         019e2bad        │
├─────────────────────────────────────────────────────────┤
│ Enter 绑定   数字选择   n 新建   m 手动输入   Esc 返回    │
└─────────────────────────────────────────────────────────┘
```

规则：

- 可选列表只包含未被其他 route 占用的 session，以及当前聊天已绑定的 session。
- 不可选列表显示 owner 中文标签。
- 手动输入已占用 session 时，展示 owner 冲突中文说明。
- 手动输入不存在 session 时，停留当前页。
- Session 标题和 ID 统一省略，详情页可显示完整 ID。

## 微信主聊天绑定

微信主聊天绑定复用 Session 选择组件，但目标不是普通 routeKey，而是微信主聊天 target。

状态：

- 已发现真实微信私聊 route：直接绑定该 route。
- 未发现真实微信私聊 route：写 pending binding。

操作：

- 选择已有 session：pending 预留该 session，收到第一条微信私聊后生效。
- `n` 新建：收到第一条微信私聊后创建新 session。
- `0` / `Esc`：暂不绑定，首条消息按新聊天策略处理。

显示：

```text
待绑定到 你好呀 / 019e3024
说明: 收到第一条微信私聊后生效。
```

## 权限设置

默认权限页：

```text
┌ 默认权限设置 ───────────────────────────────────────────┐
│ 当前: 审批模式（workspace-write 沙箱）                    │
│                                                         │
│ > 审批模式（推荐）                                      │
│   完全权限（高风险）                                    │
├─────────────────────────────────────────────────────────┤
│ Enter 保存   Esc 返回                                    │
└─────────────────────────────────────────────────────────┘
```

完全权限二次确认：

```text
完全权限会跳过审批和沙箱，可以直接执行命令并修改文件。
如确认继续，请输入 YES。
```

规则：

- 默认权限只影响以后新建的 session。
- 已绑定 session 的权限从绑定详情进入。
- session 级权限只影响当前 session 后续 turn。
- TUI 不展示 Codex adapter 模式、progress 模式、并发上限等高级项。

## 工作目录设置

工作目录页用于配置“以后新建 session 的默认工作目录”。

```text
┌ 工作目录设置 ───────────────────────────────────────────┐
│ 当前                                                     │
│  /Users/xiaohuang/codex-wechat/codex-openclaw-wechat     │
│                                                         │
│ > 使用当前终端目录                                      │
│   输入目录路径                                          │
├─────────────────────────────────────────────────────────┤
│ Enter 执行   Esc 返回                                    │
└─────────────────────────────────────────────────────────┘
```

规则：

- 默认工作目录只影响以后新建的 session。
- 绑定已有 session 时使用 Codex 历史 session 自带的 cwd，不修改已有 session。
- 聊天绑定详情只展示已有 session 的工作目录，不提供“修改当前 session 工作目录”。
- 如果用户要换工作目录，应在目标工作目录新建 session 并重新绑定这个聊天。
- 当前阶段不支持给一个 Codex session 添加多个工作目录；多工作区能力留作后续高级设计。
- 输入不存在的目录时，先显示确认提示，再创建目录并保存。
- 保存后刷新首页、启动确认页和运行日志页中的工作目录。
- 工作目录通过 actions/services 修改当前启动进程内的默认值；当前阶段不持久化固定默认目录，TUI 不直接写 `state/bridge/config.json`。

## 状态详情

状态详情用于排查，不作为主路径。

展示：

- 渠道：类型、账号、状态、启用状态、最近错误。
- 绑定：routes 数量、已绑定数量、pending 数量。
- 权限：默认权限、session 级权限数量。
- 运行：服务未启动或运行中。

状态详情可以显示完整 ID，但仍避免输出原始 JSON。

## 启动服务

首页必须把“启动服务”作为强主操作展示：

- 首页有可启动渠道时，默认选中“启动服务”。
- 首页单独展示启动状态，明确“按 Enter 启动 Bridge，并进入运行日志面板”。
- “启动服务”列表项使用更醒目的颜色；不可启动时显示需处理配置。

启动确认：

```text
┌ 即将启动 ───────────────────────────────────────────────┐
│ 渠道                                                     │
│  微信 / wx-main        已连接                            │
│  飞书 / default        已连接                            │
│                                                         │
│ 聊天绑定                                                 │
│  待生效绑定: 1                                           │
│  新聊天策略: 首条消息自动创建新 session                   │
│                                                         │
│ 权限                                                     │
│  新 session 默认权限: 审批模式                            │
├─────────────────────────────────────────────────────────┤
│ Enter 启动   Esc 返回                                    │
└─────────────────────────────────────────────────────────┘
```

第一版启动后行为：

- TUI 退出配置界面后启动 Bridge。
- TTY 默认进入 `Chat Codex 运行中` 面板，第一屏必须显示“已启动 / 运行中”状态、已启动渠道、工作目录、默认权限、新聊天策略和最近 transcript。
- 运行期日志面板显示收到消息、发送回复、本地进度和媒体发送。
- `--no-tui` 或非 TTY fallback 继续使用当前 `ConsoleTranscriptSink` 打印。

停止提示：

- 运行期日志面板必须始终显示明确停止方式：`Ctrl+C 停止服务`。
- 如果 `q` / `Esc` 没有经过真实终端验证可用，不能在 footer 写 `q` / `Esc` 停止服务。
- 第一版可以只支持 `Ctrl+C` 停止服务；后续若实现 `q` / `Esc`，必须补齐人工验证和测试记录。
- 停止服务前应尽量关闭渠道连接、停止 Bridge，并打印清晰的退出状态。

后续增强：

- 运行期 TUI 可以继续增强为左右两栏：左侧显示渠道和任务状态，右侧显示最近 transcript。
- 后续补充滚动、过滤、暂停跟随、审批状态和队列状态。

## 错误处理

通用规则：

- 用户输入错误不退出程序。
- 操作失败不丢失当前页面上下文。
- 错误文案使用中文可行动提示。
- 业务错误由 actions 返回结构化结果，TUI 只负责展示。

典型错误：

- 微信登录超时：提示继续检查或返回。
- 飞书凭证缺失：提示重新输入，不显示 secret。
- session 不存在：停留 Session 选择页。
- session owner 冲突：显示已绑定到哪个聊天。
- 无 enabled 渠道：跳转管理渠道。
- 有 enabled 渠道但未 connected：跳转对应渠道详情。

## 测试要求

Actions / Services：

- dashboard 状态汇总。
- 启动前检查。
- 微信 pending 主聊天绑定。
- 飞书添加机器人不落盘 secret。
- session 选择 owner 冲突。
- 默认权限和 session 级权限。
- 新 session 默认工作目录读取、当前进程内保存和目录创建确认。

TUI 组件：

- 首页无渠道、可启动、不可启动三种状态快照。
- 首页可启动时突出显示“启动服务”，启动确认页说明启动后进入运行中面板。
- 首页和启动确认页展示工作目录。
- 渠道列表快捷键。
- 渠道页正文直接展示“添加微信账号”“添加飞书机器人”等操作项。
- 飞书机器人表单必填账号标识，默认飞书域不阻塞普通用户。
- 飞书机器人提交后校验连通性，失败停留表单。
- 绑定列表和详情展示。
- Session 选择可选/不可选展示。
- 工作目录页输入、确认创建和返回行为。
- 错误状态栏展示。
- Esc/q 返回行为。

人工验证：

```bash
npm run chat-codex
```

重点验证：

- 窄终端和宽终端都不出现重叠。
- 中文宽字符对齐可接受。
- App Secret 输入不回显。
- 管理渠道页不用快捷键也能通过方向键和 Enter 添加微信账号、添加飞书机器人。
- 已有渠道存在时仍能继续添加第二个微信账号或飞书机器人。
- 飞书账号标识为空时不能提交；填写后按 Enter 会先校验连通性。
- 启动服务后 TTY 默认进入运行期日志面板，收到/发送/进度日志能追加显示，并展示当前工作目录。
- 运行期日志面板第一屏明确显示 `Chat Codex 已启动`。
- 运行期日志面板明确提示 `Ctrl+C 停止服务`；如果没有实现并验证，不能提示 `Esc` / `q` 可停止。
- `--no-tui` 下 transcript 正常打印。
- 非 TTY fallback 不受影响。

## 实施顺序

### T1：为 TUI 补齐 Actions

- 新增 `LauncherActions`。
- 把 dashboard 汇总、启动前检查、默认权限设置、工作目录设置、微信主聊天 pending binding 从 `serve.ts` 下沉。
- 保持现有普通 CLI 行为不变。

### T2：引入 Ink 壳

- 安装 Ink 依赖。
- 新增 TUI 入口和 App shell。
- 首页 dashboard 可刷新、可退出、可进入渠道/绑定/权限/工作目录页。
- 非 TTY 和 `--no-tui` 保留现有 prompt 交互。

### T3：渠道和绑定页

- 管理渠道页。
- 添加微信账号流程。
- 添加飞书机器人流程。
- 聊天绑定列表、详情和 Session 选择页。

### T4：启动确认和运行衔接

- TUI 启动确认页。
- 启动后退出 TUI 配置界面，进入运行期日志面板。
- 完整回归测试和真实人工验证。

### T5：运行期 TUI 可选增强

- 当前运行任务、队列和审批状态。
- 渠道健康状态实时刷新。
- 日志滚动、过滤、暂停跟随和全文查看。
